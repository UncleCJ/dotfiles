[user]
	name = CJ Sveningsson
    email = carl-johan.sveningsson@hiq.se
    # email = cj.sveningsson@hiq.se
	# email = cj.sveningsson@gmail.com
[core]
	editor = vim
	autocrlf = input
    pager = delta
	# TODO: gitconfig on windows needs cleaning
	#excludesfile = C:\\Users\\carl-johan.sveningss\\Documents\\gitignore_global.txt
	# TODO: Need to write a wrapper script to use windows tools?
	# http://stackoverflow.com/questions/1634161/how-do-i-use-notepad-or-other-with-msysgit
	# C:/Program Files (x86)/Notepad++/notepad++.exe
[alias]
	co = checkout
	st  = status
	cam = commit -a -m
	br  = branch
	co  = checkout
	logg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	# Add all tracked files to previous commit, using the that commits message
	amend = commit --amend -C HEAD
	# Add changes to be committed to previous commit, using a message you define
	amendm = commit --amend -m
	pushf = push --force-with-lease
	rh = reset --hard 
	move = mv
	copy = cp
	cloner = clone --recursive
[push]
	default = upstream
[fetch]
	prune = true
[pull]
	rebase = true
[rebase]
	autosquash = true
[rerere]
	enabled = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
# TODO: Trying to do this on windows
# http://stackoverflow.com/questions/14821358/git-mergetool-with-meld-on-windows
# [merge]
# 	tool = meld
[mergetool "meld"]
	prompt = false
	keepBackup = false
	keepTemporaries = false
	path = C:/Program Files (x86)/Meld/Meld.exe
#	cmd = '\"/c/Program Files (x86)/Meld/meld/meld.exe\" $PWD/$LOCAL $PWD/$BASE $PWD/$REMOTE --output=$PWD/$MERGED'
[difftool "nbdime"]
	cmd = git-nbdifftool diff \"$LOCAL\" \"$REMOTE\" \"$BASE\"
[mergetool "nbdime"]
	cmd = git-nbmergetool merge \"$BASE\" \"$LOCAL\" \"$REMOTE\" \"$MERGED\"
[diff "jupyternotebook"]
	command = git-nbdiffdriver diff
[merge "jupyternotebook"]
	driver = git-nbmergedriver merge %O %A %B %L %P
	name = jupyter notebook merge driver
[submodule]
	recurse = true
[delta]
    line-numbers = true
    diff-so-fancy = true
    navigate = true
